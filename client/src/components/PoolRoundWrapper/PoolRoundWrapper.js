import React from 'react';

import styles from './PoolRoundWrapper.module.scss';

const PoolRoundWrapper = () => {
    return (
        <div className={styles.PoolRoundWrapper}>
            
        </div>
    )
}

export default PoolRoundWrapper
